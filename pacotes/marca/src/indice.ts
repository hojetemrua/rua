/**
 * `@rua/marca` — tokens da marca, fonte única para os dois clientes.
 *
 * Nenhum hex escrito à mão em componente. A web usa `cssDosTokens()`; o app
 * nativo importa o objeto, porque React Native não tem variável CSS.
 */

export * from "./tokens";
export * from "./css";
